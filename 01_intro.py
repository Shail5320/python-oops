# unike proccedural programming we can create a blueprint
# of what we want and then create multiple copies of it, 
# rather than creating inefficient data structures we can
# use oop to tackle the situation in a better way

# generality to specificity 
# creating your own datatype

a = 1
b = "hello"
c = 12.34

print(type(a))
print(type(b))
print(type(c))

# A literal is simply a value that you directly write in your Python code

# now the data types of these literals are 
# also a class which is returned by the type method

# everything is an object model